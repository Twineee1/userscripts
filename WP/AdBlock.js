if (typeof BlockAds1 !== 'undefined') {/*shows by default*/
$( '.qxz-ads' ).hide();}

if (typeof BlockAds2 === 'undefined') {/*hides by default*/
$( '.vector-sitenotice-container' ).hide();}

//.vector-sitenotice-container for the big boys and
//.qxz-ads for the templates
